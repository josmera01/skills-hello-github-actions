# .github/workflows/run-shell-script.yml

name: Run Shell Script

on: [push]

jobs:
  run-shell:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run hello.sh
        run: ./hello.sh
